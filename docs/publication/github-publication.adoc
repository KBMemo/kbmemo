= KBMemo GitHub 公開準備
:toc:
:toclevels: 3

== 目的

KBMemo を構成する3つのアプリケーションを、GitHub Organization 配下の公開
repository として安全に公開するための判断基準と作業順序を定義する。

この文書は公開作業の正とする。秘密情報の監査が完了するまでは、GitHub に
public repository を作成しない。

== 公開名称

[cols="1,1,2"]
|===
|Repository |製品名 |責務

|`KBMemo/kbmemo`
|徒然
|メモ、タスク、カンバン、ノートブックを管理する本体アプリケーション

|`KBMemo/tsuzura`
|葛籠
|画像とメディアを管理し、KBMemo と Nyoy に API を提供するサービス

|`KBMemo/nyoy`
|如意
|Agent Graph、LLM、MCP、検索、画像生成を統合する AI エージェント
|===

葛籠の repository 名には一般名の `stock` ではなく、既存の API、コード、
文書で使用している製品名 `tsuzura` を使用する。

== 公開状態

状態は次の4種類を使用する。

* `TODO`: 未着手
* `DOING`: 作業中または追加確認が必要
* `DONE`: 完了し、公開判定の証跡がある
* `BLOCKED`: 外部作業または方針決定待ち

[cols="1,1,1,1"]
|===
|項目 |徒然 |葛籠 |如意

|GitHub Organization / repository の確保
|BLOCKED
|BLOCKED
|BLOCKED

|履歴を含む secret scan
|DONE: gitleaks 8.30.1
|DONE: gitleaks 8.30.1
|DONE: gitleaks 8.30.1

|ライセンス
|DONE: MIT
|DONE: MIT
|DONE: MIT

|README とセットアップ手順
|DOING: GitHub link更新済み
|DOING: GitHub link・配置例更新済み
|DOING: GitHub link・接続初期値更新済み

|内部インフラ情報の一般化
|TODO
|TODO
|TODO

|GitHub Actions
|DOING
|DOING
|DOING

|`SECURITY.md`
|TODO
|TODO
|TODO

|`CONTRIBUTING.md`
|TODO
|TODO
|TODO

|Private repository への移行確認
|TODO
|TODO
|TODO

|Public 化
|BLOCKED
|BLOCKED
|BLOCKED
|===

== 2026-07-27 事前監査

=== 共通

* 実 `.env`、`config/master.key`、`config/credentials.yml.enc` は Git の追跡対象外。
* HEAD の追跡ファイルに、単純な資格情報代入パターンへ一致する実値は検出されなかった。
* 5 MiB を超えるファイルは HEAD に存在しない。
* `.github/workflows/ci.yml` と Dependabot 設定が存在する。
* `gitleaks 8.30.1` の公式配布archiveをchecksum検証し、全Git履歴を
  `--redact` 付きで検査した。
* 検出結果に実秘密情報は確認されなかった。公開前に同じ固定バージョンで再実行する。

=== 徒然

* MIT License が存在する。
* ローカル`main`とGiteaの`origin/main`は同期済み。
* `README.md` と `README.adoc` が併存している。公開時の正を決める。
* npm package publish workflow は registry と token の GitHub Actions secrets を必要とする。
* Gitea URL、内部ホスト名、個人環境のパスを含む文書と設定がある。
* 公開repository間リンクとnpm package metadataは`github.com/KBMemo/*`へ更新済み。
* gitleaksは2件を検出した。
** `app/services/tsuzura/client.rb`: secretを取得するメソッド呼び出しの誤検知。
** `docs/deployment/production.adoc`: nginx設定内の証明書private keyファイルパス。
   key本体ではない。

=== 葛籠

* MIT Licenseを採用した。
* 現在の repository 名は `kbmemo-media`、製品名は Tsuzura（葛籠）。
* Gitea URLと内部環境向け deploy 設定がある。
* READMEとAGENTSの設計リンク、systemd配置例、deployヘルプはGitHubと
  `/srv/kbmemo/tsuzura`を使う公開向け表記へ更新済み。
* gitleaksの検出は0件。

=== 如意

* Nyoy本体と`agent_graph-core`はMIT Licenseを採用した。
* `agent_graph-core.gemspec` のhomepageは`KBMemo/nyoy`を参照するよう更新済み。
* 一部の個別runbookには、内部ホスト名、private network のアドレス、
  個人環境のパスが残っている。
* README、example env、database、LLM / SD / 検索 / readabilityのruntime defaultは
  `localhost`基準へ一般化した。既存環境のDBホストはGit管理外credentialsへ移行した。
* `docs/production-setup.md`とsystemd unitは、`app.example.com`、
  `db.example.com`、`/srv/kbmemo/nyoy`を使う公開用手順へ一般化済み。
* Memo RAG webhook runbookは例示URLと`/srv/kbmemo/*`配置へ一般化済み。
  ImageUnderstanding runbookは手順と記録テンプレートだけを公開対象とし、
  endpoint、media ID、AgentRun IDを含む実機ログをrepository外へ分離した。
* llama-switchd runbookはcontrol/data plane、binding、lifecycle、
  reconciliation、alertの運用契約へ再構成し、内部inventory、revision、
  Zabbix object IDを含む実機日誌をrepository外へ分離した。
* MCP server文書のローカル絶対パスと本番URLを例示値へ変更した。
  Chat reasoning runbookのendpointと測定結果はrepository外の運用記録へ
  分離した。
* 徒然API要件からローカルcloneの配置先とDB hostを除去した。PGroonga文書は
  production固有の実施日、revision、検索語を汎用導入チェックリストへ変更した。
* LLM接続アーキテクチャ文書の内部hostとproduction移行記録を除去し、
  portだけでbindingしない制約と用途assignment監査の契約へ一般化した。
* 徒然・葛籠への文書リンクは`github.com/KBMemo/*`へ更新済み。
* remote deployは`bin/deploy-remote`へ一般化し、`NYOY_DEPLOY_HOST`と
  `NYOY_DEPLOY_PATH`を必須化した。旧`bin/deploy-bowmore`は個人環境の既定値を
  持たないdeprecated wrapperとして維持する。
* 履歴書き換え後のgitleaks検出は6件。
** すべてrunbookの`Authorization: Bearer your-token`という例示値。
** 過去の`config/credentials.yml.enc`とその暗号文は全refから除去済み。
** `config/master.key`が追跡されたcommitは0件であり、復号keyの漏えいは確認されなかった。

==== credentials履歴除去

2026-07-27にNyoyの全refを一時mirrorで検証後、復旧用bundleを作成・検証し、
実repositoryへ履歴書き換えを適用した。

* `git-filter-repo --sensitive-data-removal --invert-paths
  --path config/credentials.yml.enc`
* 対象commit: 394
* 書き換わるcommit SHA: 394
* 削除されるcommit: 0
* `config/credentials.yml.enc`が残るcommit: 0
* 書き換え前後の`main` HEAD tree: 一致
* 書き換え後のgitleaks検出: 6件
** すべてrunbookの`Authorization: Bearer your-token`例示
** 暗号化credentialsの検出は解消

最初に変更されるcommitは
`7fbc77ef6be898035a5a02772f26484dc0d31afa`。
Giteaの旧`main` SHAを明示した`--force-with-lease`で更新し、localとremoteの
`main`一致を確認した。既存cloneは再取得し、旧Gitea refや古いcloneから
書き換え前の履歴を再流入させない。

== 公開ゲート

次の条件をすべて満たすまで public にしない。

. 使用中または過去に使用した token、password、private key をすべて失効または
  rotation している。
. 全 branch、tag、commit history を secret scanner で検査し、結果を保存している。
. 検出された実秘密情報を `git-filter-repo` 等で全 ref から除去している。
. ライセンスと third-party dependency の配布条件を確認している。
. README だけで development 環境を構築できる。
. 内部 DNS 名、private IP、個人の絶対パス、本番 token の取得手順を一般化している。
. GitHub Actions が fork からの pull request でも秘密情報なしで安全に実行できる。
. `SECURITY.md` に非公開の脆弱性報告経路がある。
. GitHub の Private repository で CI、リンク、README 表示を確認している。

== 作業順序

=== Phase 1: 名称確保

. GitHub に `KBMemo` Organization を作成する。
. `kbmemo`、`tsuzura`、`nyoy` を空の Private repository として作成する。
. README、LICENSE、`.gitignore` はGitHub側で自動生成しない。

=== Phase 2: 公開監査

. `gitleaks` を固定バージョンで導入する。
. working tree と全 Git history を検査する。
. 検出結果を、値を含めず repository ごとの監査記録へ残す。
. 実秘密情報が見つかった場合は、先に資格情報を失効してから履歴を書き換える。
. Gitea 上の古い ref と clone から秘密情報が再流入しない運用を決める。

=== Phase 3: 公開向け整備

. 3 repository の LICENSE を統一する。
. README に製品間の関係、依存サービス、最小起動手順を追加する。
. Gitea URLをGitHub URLへ変更する。
. deploy と runbook のホスト固有値を環境変数または例示名へ置き換える。
. `SECURITY.md` と `CONTRIBUTING.md` を追加する。
. repository description、topics、social preview を準備する。

=== Phase 4: GitHub 移行

. 徒然の未 push commit を先にGiteaへ反映する。
. 空のGitHub repositoryへ全 branchとtagをmirrorする。
. 通常の作業remoteを次の構成へ変更する。

[source,text]
----
origin  git@github.com:KBMemo/<repository>.git
gitea   git@gitea.artif.org:Artif.org/<old-repository>.git
----

. default branchを`main`に設定する。
. branch protection、Dependabot、Actions permissionsを設定する。
. GitHubを正、Giteaをbackupまたはread-only mirrorとして扱う。

=== Phase 5: 公開

. Private状態で各repositoryの`bin/ci`を実行する。
. READMEの相互リンクとclone手順を確認する。
. 葛籠、如意、徒然の順でpublicにし、最後に統合動作を確認する。
. 最初のrelease tagを作成し、既知の制限をrelease notesへ記載する。

== 次の作業

. 内部環境情報を、削除、一般化、公開可能な運用例のいずれに分類する。
. 3 repositoryのREADMEと公開リンクをGitHub構成へ合わせる。
