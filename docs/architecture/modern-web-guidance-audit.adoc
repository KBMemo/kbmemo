= Modern Web Guidance ベースの Web 実装監査メモ
:toc:

このメモは、2026-06-11 時点の `kbmemo_site` を、Chrome の Modern Web Guidance を軸に
HTML / CSS / JavaScript / accessibility / performance / security の観点で見直し、
**実装修正の優先順位**を整理したものです。

== 参照

* Chrome for Developers, "Get started with Modern Web Guidance"
  https://developer.chrome.com/docs/modern-web-guidance/get-started
* Chrome for Developers, "Explore Modern Web Guidance skills"
  https://developer.chrome.com/docs/modern-web-guidance/explore-skills
* MDN, "HTML: A good basis for accessibility"
  https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Accessibility/HTML

Modern Web Guidance は、semantic HTML、native interactive APIs、focus management、resource prioritization、
XSS/CSP/cookies などを中心に、モダンな Web 実装へ寄せるための指針群として整理されている。
本監査もその分類に合わせる。

== 実施ログ

=== 2026-06-11

* CSP は `config/initializers/content_security_policy.rb` で report-only として有効化。既存ページの違反収集を前提にし、enforcement は次段階に残す。
* `application.html.slim` / `diagram_viewer.html.slim` のテーマ初期化 inline script を削除し、`application` entrypoint が `script[type="application/json"]` の account theme payload を読む構成へ変更。
* `memo_ai_panel_controller.js` の error 表示を `innerHTML` から `textContent` / `createElement` ベースへ変更。
* `application.html.slim` / `diagram_viewer.html.slim` / `svg_editor.html.slim` に `lang="ja"` を付与。
* `application.html.slim` に skip link と `main#main-content` を追加。
* Tsuzura picker dialog に `aria-labelledby` / `aria-describedby` を追加。
* `code_block_tools_controller.js` の sanitize 済み SVG 注入を `replaceWithSanitizedSvg` に隔離し、loading / error 表示を DOM API ベースへ変更。
* `user_menu_controller.js` に `aria-controls`、open 時 focus、Escape focus return、Arrow/Home/End navigation を追加。
* ディレクトリ dialog に `aria-labelledby` を追加し、server-rendered fragment の `innerHTML` 代入を `replaceWithServerRenderedForm` に隔離。
* サイドバー resizer を keyboard 操作可能な separator にし、Arrow/Home/End resize と `aria-valuenow` 同期を追加。
* route 固有の Stimulus controller（memo editor、diagram viewer/editor、theme/skin studio、memo show 補助 UI 等）を dynamic import 登録へ変更し、該当 DOM があるページだけ読み込む構成へ寄せた。
* メモ本文 syntax highlight は `@kbmemo/adoc-codemirror` package root ではなく `codeHighlight` 単体を dynamic import し、閲覧ページで不要な Asciidoctor / CodeMirror 系依存を初期 bundle から外した。
* `bundle exec vite build --force` で ineffective dynamic import 警告が消え、`application` chunk は約 1.79 MB から約 491.86 kB まで縮小した。`svg_editor` と editor/preview 系 async chunk の size warning は残る。
* SVG editor entry から `svgedit` 本体と CSS の static import を外し、`memo_svg_editor_controller.js` 接続時の dynamic import に変更。`svg_editor` entry は約 2.19 MB から約 2.12 kB まで縮小し、SVGEdit 本体は `Editor` async chunk に分離した。
* editor / preview 系 import を package root から必要 module の direct import へ寄せ、`@kbmemo/adoc-kbmemo` の不要 export 巻き込みを削減した。`adoc-kbmemo` async chunk は約 1.10 MB から約 344.96 kB まで縮小した。
* `scripts/report-vite-bundles.mjs` / `npm run report:bundles` を追加し、Vite manifest から JS / CSS bundle size と gzip size を確認できるようにした。
* release 前の Lighthouse / CWV / bundle size 確認手順を `docs/architecture/web-performance-checklist.adoc` に追加した。
* Rails model form の validation error 表示に `aria-invalid` / `aria-describedby` を付与する共通 helper を追加し、memo / memo directory / board / notebook / tag / profile の主要 field に適用した。
* `render_diagram` endpoint の成功レスポンスに `X-Kbmemo-Svg-Sanitized: MemoSvgSanitizer` / `X-Content-Type-Options: nosniff` / `Cache-Control: no-store` と `sanitized: true` を付与し、フロント側はこの契約を満たす SVG だけを挿入するようにした。

== 現状の良い点

* semantic な `button` / `details` / `dialog` をすでに多用している。
* flash 通知に `aria-live="polite"` が入っている。
* avatar と本文画像系には `loading="lazy"` / `decoding="async"` が使われている。
* production では `force_ssl = true`、host 制限、cookie store を使っている。
* Tailwind 依存を外し、テーマ設計を `themes.css` と Open Props に寄せている。

== 優先度付き更新案

[cols="1,1,3,4",options="header"]
|===
|優先度 |領域 |現状 |更新案

|P0
|Security
|CSP は report-only で有効化済み。`application.html.slim` / `diagram_viewer.html.slim` のテーマ初期化 inline script は削除済み。
|report-only 違反を確認し、必要な fetch / style / asset origin を整理してから enforcement へ移行する。

|P0
|Security
|`memo_ai_panel_controller.js` の error 表示は DOM API ベースへ置換済み。`messagesTarget.innerHTML` は local escape 済み render として残る。
|残る `innerHTML` は「信頼済み・サニタイズ済み HTML を入れる箇所」に限定し、関数名やコメントで境界を明示する。

|P0
|Security
|`code_block_tools_controller.js` の SVG 注入は `replaceWithSanitizedSvg` に隔離済み。loading / error 表示は DOM API ベースへ変更済み。`render_diagram` は sanitize 済みヘッダー / JSON flag を返し、controller test で固定済み。
|今後 Trusted Types を検討する場合は、この sink を named policy 化する候補にする。

|P1
|Accessibility / HTML
|`application.html.slim`、`diagram_viewer.html.slim`、`svg_editor.html.slim` は `lang="ja"` 付与済み。
|将来 i18n を広げるなら locale 連動にする。

|P1
|Accessibility / HTML
|`application.html.slim` は skip link と `main#main-content` 追加済み。
|長いナビゲーションを持つ layout が増えた場合は同じ pattern を使う。

|P1
|Accessibility / JS
|`user_menu_controller.js` は `aria-controls`、open 時の初期 focus、Escape の focus return、menuitem の Arrow/Home/End 移動を追加済み。
|select は native 操作を優先して Tab 移動対象に残す。今後 popover 化する場合は role 設計も再評価する。

|P1
|Accessibility / HTML/JS
|Tsuzura picker は `aria-labelledby` / `aria-describedby` 付与済み。ディレクトリ dialog は `aria-labelledby` 付与済みで、fragment 代入も専用メソッドに隔離済み。
|補助説明が必要な dialog が増えた場合は `aria-describedby` も追加する。

|P1
|Accessibility
|サイドバー resizer は focusable separator として Arrow/Home/End で幅変更可能。`aria-valuemin` / `aria-valuemax` / `aria-valuenow` も同期済み。
|必要に応じて操作説明のヘルプ文や `aria-valuetext` を追加する。

|P1
|Forms / Accessibility
|Rails model form の主要 field は、server validation error 時に `aria-invalid="true"` と field error message の `aria-describedby` を付与済み。既存の error summary は残している。
|今後追加する form も `kb_field_error_aria` / `kb_field_error_id` を使う。client-side validation を増やす場合は `:user-invalid` と ARIA 状態の同期を別途検討する。

|P1
|Performance / JS
|route 固有の Stimulus controller は lazy 登録へ変更済み。メモ本文 highlight も `codeHighlight` 単体 dynamic import に切り出し済み。SVG editor も entry と SVGEdit 本体を分離済み。editor / preview 系は package root import を避ける形へ寄せた。直近の `bundle exec vite build --force` では `application` が約 491.99 kB、`svg_editor` entry が約 2.12 kB、`adoc-kbmemo` async chunk が約 344.96 kB。ineffective dynamic import 警告は解消済み。
|次は CodeMirror / Asciidoctor 側の async chunk 境界を検討する。現状は source editor 初期化に必要なため、無理に割るより測定と UX 影響を見て判断する。

|P1
|Performance / HTML/JS
|テーマ初期化 inline script は削除済み。account theme payload は JSON script tag で渡している。
|今後は初期描画前のテーマちらつきと CSP enforcement の両立を検証する。

|P2
|CSS
|`utility-compat.css` が Tailwind 置換レイヤとして成長している。現状は実用上問題ないが、長期では再び utility 膨張に戻りやすい。
|「互換 layer は縮小専用」と決め、**新規 utility 追加を抑制**する。原則は semantic class を `themes.css` に置く。lint かレビュー項目で固定するとよい。

|P2
|HTML / Accessibility
|現在も semantic HTML は比較的良いが、custom popup / picker の増加で native 機能との差が広がりやすい。
|Baseline を明示した上で、可能な箇所は `dialog` / `details` に寄せ、将来的には `popover` も検討する。Modern Web Guidance でも native overlays と focus management が中心テーマになっている。

|P2
|Performance
|`docs/architecture/web-performance-checklist.adoc` と `npm run report:bundles` を追加済み。
|release 前に LCP / INP / CLS と bundle size を確認する。必要なら CI の artifact / threshold に昇格する。
|===

== 具体的な観察メモ

=== 1. Layout / HTML

* `app/views/layouts/application.html.slim`
** `lang="ja"` 付与済み。
** skip link と `main#main-content` 追加済み。
** theme bootstrap inline script は削除済み。
* `app/views/layouts/diagram_viewer.html.slim`
** `lang="ja"` 付与済み。
** theme bootstrap inline script は削除済み。
* `app/views/layouts/svg_editor.html.slim`
** `lang="ja"` 付与済み。

=== 2. Dialog / Menu

* `app/views/memos/_tsuzura_picker.html.slim`
** `aria-labelledby` / `aria-describedby` 接続済み。
* `app/javascript/controllers/user_menu_controller.js`
** 排他 open/close はできている。
** open 後の focus 遷移、Escape 時の focus return、Arrow/Home/End navigation を追加済み。
* `app/javascript/controllers/memo_directory_dialog_controller.js`
** native `showModal()` を使っている点は良い。
** `replaceWithServerRenderedForm` に隔離済み。server-rendered fragment 前提の信頼境界はコードコメントで明示。

=== 3. Security sink

* `app/javascript/controllers/memo_ai_panel_controller.js`
** `messagesTarget.innerHTML` は local render で escape 済み。
** error 表示は DOM API ベースへ置換済み。
* `app/javascript/controllers/code_block_tools_controller.js`
** SVG 注入は `replaceWithSanitizedSvg` に隔離済み。`render_diagram` endpoint の response header / JSON flag / controller test で sanitize 契約を明示済み。
* `app/javascript/controllers/memo_search_picker_controller.js`
** こちらは `_escape` / `_escapeAttr` を入れており、比較的丁寧。

=== 3.5. Forms / Accessibility

* Rails model form
** `kb_field_error_aria` / `kb_field_error_id` / `kb_field_error_message` を追加済み。
** memo / memo directory / board / notebook / tag / profile の主要 field は server validation error 時に field 単位で error text を参照する。
** Rodauth form は既に field error を `aria-invalid` / `aria-describedby` へ接続済み。

=== 4. Performance

* Vite build は成功するが、CodeMirror と SVGEdit 本体の async chunk size warning は継続している。
* `application` 初期 chunk は約 491.99 kB まで縮小済み。
* `svg_editor` entry は約 2.12 kB まで縮小済み。SVGEdit 本体は `Editor` async chunk として、`memo-svg-editor` 接続後に読み込む。
* `adoc-kbmemo` async chunk は package root import を避けることで約 344.96 kB まで縮小済み。
* `packages/adoc-codemirror/index.js` の package root を本文閲覧 highlight から外したため、閲覧ページの syntax highlight は `codeHighlight` chunk のみを遅延取得する。
* editor 系の JS は `memo-body-editor` controller が存在する画面でのみ読み込む構成へ寄せた。

== 推奨実施順

=== Phase 1

* DONE: CSP を report-only で有効化する
* DONE: theme bootstrap の inline script を外部化する
* DONE: `memo_ai_panel_controller.js` の error `innerHTML` を除去する

=== Phase 2

* DONE: `lang="ja"` を全 layout に付与する
* DONE: skip link を追加する
* DONE: dialog の accessible name / description を揃える（Tsuzura は `aria-labelledby` / `aria-describedby`、ディレクトリ dialog は `aria-labelledby` 済み）
* DONE: resizer のアクセシビリティ方針を決め、keyboard resize を実装する

=== Phase 3

* DONE: bundle 分割を見直す（Stimulus controller lazy 登録、本文 highlight の単体 dynamic import）
* DONE: ineffective dynamic import を解消する
* DONE: SVG editor entry と SVGEdit 本体を分離する
* DONE: editor / preview 系の package root import を direct import へ寄せる
* TODO: CodeMirror / Asciidoctor async chunk の測定と追加分割可否を検討する

=== Phase 4

* DONE: Rails model form の validation error を field 単位で `aria-invalid` / `aria-describedby` に接続する
* `utility-compat.css` を縮小運用へ切り替える
* DONE: Lighthouse / CWV / bundle size の確認手順を docs に追加する
* DONE: Vite bundle size report script を追加する

== 補足

Modern Web Guidance は、最新機能を無条件に採用すること自体を目的にしていない。
`Baseline` と fallback を前提に、**native で済むものは native に寄せ、JS は必要なところだけに限定する**
という方向が中心にある。`kbmemo_site` では、すでに `dialog` / `details` / semantic button が入っているため、
全面作り直しではなく、**CSP・focus management・bundle 分割**の3本を先に締めるのが最も効果的である。
