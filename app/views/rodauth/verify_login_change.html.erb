<%= form_with url: rodauth.verify_login_change_path, method: :post, data: { turbo: false }, class: "w-full max-w-sm" do |form| %>
  <%= form.submit rodauth.verify_login_change_button, class: kb_auth_submit_classes %>
<% end %>
