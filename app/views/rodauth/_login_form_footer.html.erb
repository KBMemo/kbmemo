<% unless rodauth.login_form_footer_links.empty? %>
  <ul>
    <% rodauth.login_form_footer_links.sort.each do |_, link, text| %>
      <li><%= link_to text, link, class: "kb-chrome-link text-sm underline-offset-2 hover:underline #{kb_focus_ring}" %></li>
    <% end %>
  </ul>
<% end %>
