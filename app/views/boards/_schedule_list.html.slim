.kb-board-schedule-list.flex.min-h-0.flex-1.flex-col.overflow-hidden.px-3.py-3
  .mb-2.flex.items-center.justify-between.gap-2
    h2.text-xs.font-medium.uppercase.tracking-wide.kb-text-muted 予定
    p.text-xs.kb-text-secondary = l(calendar.selected_day, format: :long)
  .min-h-0.flex-1.overflow-y-auto
    - if calendar.list_items.any?
      ul.space-y-2
        - calendar.list_items.each do |memo|
          li.rounded-md.border.kb-border.px-2.py-2
            = link_to memo.title, memo_path(memo), class: "block text-sm font-medium kb-text-primary hover:opacity-80 line-clamp-2", data: { turbo: false }
            - meta = board_schedule_item_meta(memo)
            - if meta.present?
              p.mt-1.text-[10px].kb-text-muted = meta
    - else
      p.text-sm.kb-text-muted この日の予定はありません。
      p.mt-1.text-xs.kb-text-subtle カンバンカードの予定日や Google Calendar から取り込んだ予定がここに表示されます。
