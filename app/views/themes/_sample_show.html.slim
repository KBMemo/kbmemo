.max-w-none
  article.memo-body.asciidoctor.kb-card.p-6 data-theme-slot="memo-body"
    h1 サンプルメモ
    p
      | これはテーマプレビュー用の
      strong 表示
      | サンプルです。AsciiDoc の見た目とメモカードのスタイルを確認できます。
    .admonitionblock.note
      table
        tbody
          tr
            td.icon
              i.fa.icon-note[title="Note" aria-hidden="true"]
              | Note
            td.content
              | テーマ作成ページでは Design モードで要素をクリックし、スタイルを編集できます。
    pre
      code
        | def hello
        |   puts "kbmemo"
        | end
    blockquote
      p
        | 引用ブロックのサンプルテキストです。
